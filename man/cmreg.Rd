% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmreg.R
\name{cmreg}
\alias{cmreg}
\title{cmreg}
\usage{
cmreg(
  formula,
  anchor,
  p,
  p.prime,
  data,
  start = NULL,
  n.start = 3L,
  control = list()
)
}
\arguments{
\item{formula}{an object of class "formula":
a symbolic description of the model to be fitted.
Ex. Crosswise response ~ Covariates. The anchor response is supplied
separately through `anchor`.}

\item{anchor}{Unquoted column name (or a single character column name) for the
anchor response.}

\item{p}{an auxiliary probability for the crosswise question.}

\item{p.prime}{an auxiliary probability for the anchor question.}

\item{data}{a data frame containing information from the crosswise model and covariates.}

\item{start}{Optional numeric vector of starting values for the beta and theta
parameters. By default, starts are derived from binomial GLMs for the observed
crosswise and anchor responses.}

\item{n.start}{Number of optimization starts, including the data-informed start.}

\item{control}{A list of control settings passed to [stats::optim()]. `fnscale`
is fixed internally because the log-likelihood is maximized.}
}
\value{
An object of class `cmreg`, a list with:
\describe{
  \item{Call}{The matched model call.}
  \item{Coefficients}{A coefficient table for the latent-trait outcome model.}
  \item{AuxiliaryCoef}{A coefficient table for the anchor-response model.}
  \item{VCV}{The estimated variance-covariance matrix of all parameters.}
  \item{estimates, std.errors}{Named full-precision parameter estimates and
  standard errors.}
  \item{logLik, n, convergence}{The optimized log-likelihood, number of
  complete observations, and optimizer convergence code.}
}
}
\description{
\code{cmreg} is used to run a regression with the latent sensitive trait as an outcome.
}
\examples{
m <- cmreg(Y ~ female + age, anchor = A, p = 0.1, p.prime = 0.15,
           data = cmdata2)
m
}
\references{
Atsusaka, Y. and Stevenson, R. T. (2023). The crosswise model
for sensitive survey questions. \doi{10.1017/pan.2021.43}.
}
\seealso{
[cmpredict()] for predicted latent-trait probabilities and
[cmreg_p()] for a model with the latent trait as a predictor.
}
