% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmreg.p.R
\name{cmreg_p}
\alias{cmreg_p}
\alias{cmreg.p}
\title{cmreg_p}
\usage{
cmreg_p(
  formula,
  crosswise,
  anchor,
  p,
  p.prime,
  data,
  start = NULL,
  n.start = 3L,
  control = list()
)

cmreg.p(...)
}
\arguments{
\item{formula}{an object of class "formula":
a symbolic description of the model to be fitted.
Ex. Outcome ~ Covariates. The crosswise and anchor responses are supplied
separately through `crosswise` and `anchor`.}

\item{crosswise}{Unquoted column name (or a single character column name) for
the crosswise response.}

\item{anchor}{Unquoted column name (or a single character column name) for the
anchor response.}

\item{p}{an auxiliary probability for the crosswise question.}

\item{p.prime}{an auxiliary probability for the anchor question.}

\item{data}{a data frame containing information from the crosswise model, the outcome variable, and covariates.}

\item{start}{Optional numeric vector of starting values on the reporting scale:
beta, theta, gamma, crosswise effect, and a positive sigma. By default, starts
are derived from GLM and least-squares fits to observed responses.}

\item{n.start}{Number of optimization starts, including the data-informed start.}

\item{control}{A list of control settings passed to [stats::optim()]. `fnscale`
is fixed internally because the log-likelihood is maximized.}
}
\value{
A list containing the estimated results and related statistics.
}
\description{
\code{cmreg} is used to run a regression with the latent sensitive trait as a predictor.
}
\examples{
m2 <- cmreg_p(V ~ age + female, crosswise = Y, anchor = A, p = 0.1,
              p.prime = 0.15, data = cmdata3)
m2
}
